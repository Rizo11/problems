#include <stdio.h>

int main()
{
    int t = 0, length = 0;
    scanf("%d", &t);
    scanf("%d", &length);
    for (int i = 0; i < t; i++)
    {
        int a = 0, b = 0, c = 0, d = 0, e = 0;
        for (int j = 0; j < length-3; j++)
        {
            scanf("%d %d %d", &c, &d, &e);
            if ((b+c+d)%3)
            {
                printf("YES");
                break;
            }
            a = 
            
        }
    }
    
}